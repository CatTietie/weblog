import { defineComponent as y, createVNode as f, reactive as H, shallowRef as $, computed as b, onMounted as x, onBeforeUnmount as O } from "vue";
import { throttle as w } from "@vavt/util";
import { p as T } from "./chunks/config.mjs";
import { g as S, b as I, C as L, P as N } from "./chunks/index4.mjs";
const A = {
  tocItem: {
    type: Object,
    default: () => ({})
  },
  mdHeadingId: {
    type: Function,
    default: () => {
    }
  },
  scrollElement: {
    type: [String, Object],
    default: ""
  },
  onClick: {
    type: Function,
    default: () => {
    }
  },
  scrollElementOffsetTop: {
    type: Number,
    default: 0
  }
}, C = /* @__PURE__ */ y({
  props: A,
  setup(l) {
    return () => {
      const {
        tocItem: o,
        mdHeadingId: i,
        scrollElement: n,
        onClick: u,
        scrollElementOffsetTop: p
      } = l;
      return f("div", {
        class: [`${T}-catalog-link`, o.active && `${T}-catalog-active`],
        onClick: (s) => {
          u(s, o), s.stopPropagation();
          const g = i(o.text, o.level, o.index), d = document.getElementById(g), e = n instanceof Element ? n : document.querySelector(n);
          if (d && e) {
            let t = d.offsetParent, c = d.offsetTop;
            if (e.contains(t))
              for (; t && e != t; )
                c += t == null ? void 0 : t.offsetTop, t = t == null ? void 0 : t.offsetParent;
            e == null || e.scrollTo({
              top: c - p,
              behavior: "smooth"
            });
          }
        }
      }, [f("span", {
        title: o.text
      }, [o.text]), f("div", {
        class: `${T}-catalog-wrapper`
      }, [o.children && o.children.map((s) => f(C, {
        mdHeadingId: i,
        key: `${o.text}-link-${s.level}-${s.text}`,
        tocItem: s,
        scrollElement: n,
        onClick: u,
        scrollElementOffsetTop: p
      }, null))])]);
    };
  }
}), M = C, F = {
  /**
   * 编辑器的Id，务必与需要绑定的编辑器Id相同
   */
  editorId: {
    type: String
  },
  class: {
    type: String,
    default: ""
  },
  mdHeadingId: {
    type: Function,
    default: (l) => l
  },
  /**
   * 指定滚动的容器，选择器需带上对应的符号，默认预览框
   * 元素必须定位！！！！！！
   *
   * 默认：#md-editor-preview-wrapper
   */
  scrollElement: {
    type: [String, Object]
  },
  theme: {
    type: String,
    default: "light"
  },
  /**
   * 高亮标题相对滚动容器顶部偏移量，即距离该值时，高亮当前目录菜单项
   *
   * 默认：20px
   */
  offsetTop: {
    type: Number,
    default: 20
  },
  /**
   * 滚动区域的固定顶部高度
   *
   * 默认：0
   */
  scrollElementOffsetTop: {
    type: Number,
    default: 0
  },
  onClick: {
    type: Function
  }
}, P = /* @__PURE__ */ y({
  name: "MdCatalog",
  props: F,
  emits: ["onClick"],
  setup(l, o) {
    const i = l.editorId, n = H({
      list: [],
      show: !1,
      scrollElement: l.scrollElement || `#${i}-preview-wrapper`
    }), u = $(), p = b(() => {
      const e = [];
      return n.list.forEach((t, c) => {
        const {
          text: E,
          level: h
        } = t, a = {
          level: h,
          text: E,
          index: c + 1,
          active: u.value === t
        };
        if (e.length === 0)
          e.push(a);
        else {
          let r = e[e.length - 1];
          if (a.level > r.level)
            for (let m = r.level + 1; m <= 6; m++) {
              const {
                children: v
              } = r;
              if (!v) {
                r.children = [a];
                break;
              }
              if (r = v[v.length - 1], a.level <= r.level) {
                v.push(a);
                break;
              }
            }
          else
            e.push(a);
        }
      }), e;
    }), s = () => n.scrollElement instanceof HTMLElement ? n.scrollElement : document.querySelector(n.scrollElement), g = w((e) => {
      if (e.length === 0)
        return n.list = [], !1;
      const {
        activeHead: t
      } = e.reduce((c, E, h) => {
        const a = document.getElementById(l.mdHeadingId(E.text, E.level, h + 1));
        if (a instanceof HTMLElement) {
          const r = s(), m = S(a, r);
          if (m < l.offsetTop && m > c.minTop)
            return {
              activeHead: E,
              minTop: m
            };
        }
        return c;
      }, {
        activeHead: e[0],
        minTop: Number.MIN_SAFE_INTEGER
      });
      u.value = t, n.list = e;
    }), d = () => {
      g(n.list);
    };
    return x(() => {
      var t;
      const e = s();
      (t = e === document.documentElement ? window : e) == null || t.addEventListener("scroll", d), I.on(i, {
        name: L,
        callback: (c) => {
          g(c);
        }
      }), I.emit(i, N);
    }), O(() => {
      var t;
      const e = s();
      (t = e === document.documentElement ? window : e) == null || t.removeEventListener("scroll", d);
    }), () => f("div", {
      class: `${T}-catalog${l.theme === "dark" ? "-dark" : ""} ${l.class}`
    }, [p.value.map((e) => f(M, {
      mdHeadingId: l.mdHeadingId,
      tocItem: e,
      key: `link-${e.level}-${e.text}`,
      scrollElement: n.scrollElement,
      onClick: (t, c) => {
        l.onClick ? l.onClick(t, c) : o.emit("onClick", t, c);
      },
      scrollElementOffsetTop: l.scrollElementOffsetTop
    }, null))]);
  }
}), k = P;
k.install = (l) => (l.component(k.name, k), l);
export {
  k as default
};
